# 技术债与内容债

**生成日期**: 2026-05-18
**仓库 HEAD**: `fb406d6`

本文件记录下一阶段需要还的债。它不是一次性重构清单，而是小步推进时的风险地图。

## P0 债务

| 类别     | 债务                                               | 当前证据                                                                                     | 处理策略                                       |
| -------- | -------------------------------------------------- | -------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| 内容模型 | 旧 `coachEligibleIds.json` 已不再作为运行时事实源  | 已拆成 `coachBasicEligibleIds` / `coachReadyIds` / `variationGroups`，仍保留旧文件作迁移兼容 | 后续确认脚本不再依赖后再删除旧文件             |
| 内容深度 | 3013 道题仍缺 `solutionSequence` / `wrongBranches` | P0-D 首批 20 道已补齐；`quality` 抽样仍提示大范围复审                                        | 继续小批量补高价值题，不批量生成后直接上线     |
| 内容结构 | 3033 题全部 19×19，难度 3/4 与 `tesuji` 过度集中   | `puzzle-audit` 分布报告                                                                      | 新增内容优先 9×9 / 13×13、官子、布局           |
| 重复治理 | 89 个部分重复组、243 道题                          | `duplicates` 报告                                                                            | 整理为同形题 / 变化题，差异不明时再考虑去重    |
| 文档同步 | 测试数量、报告时间、Coach 语义容易漂移             | `OPERATIONS_QA` 曾停留在 86 / 689                                                            | 把 PROJECT_STATUS 作为阶段事实源，变更后跑验证 |

## P1 债务

| 类别     | 债务                                             | 风险                         | 处理策略                                           |
| -------- | ------------------------------------------------ | ---------------------------- | -------------------------------------------------- |
| 学习闭环 | onboarding、result、review、stats 还像功能页集合 | 用户完成一题后不知道下一步   | 先实现最小推荐闭环，再扩展指标                     |
| 错因模型 | 当前只有对错，没有错因                           | 复习和 Coach 无法归因        | 从 5 个错因枚举开始，先做启发式                    |
| Coach UI | 目前只有聊天框和限额错误                         | 用户不知道当前题支持什么能力 | 按内容层级展示能力边界和建议问题                   |
| 转化点   | 登录 / Pro 引导容易泛化                          | 用户还没感到学习价值就被打断 | 只在完成题目、想保存、配额耗尽、需要高级变例时引导 |

## P2 债务

| 类别       | 债务                                               | 风险                 | 处理策略                                      |
| ---------- | -------------------------------------------------- | -------------------- | --------------------------------------------- |
| 发布材料   | GitHub 展示、case study、launch checklist 未系统化 | 难以对外解释项目价值 | P2 统一整理                                   |
| 商业化文案 | 需避免不可证明承诺                                 | 合规与信任风险       | 审计“无限、保证、专业级”等词                  |
| 运营观测   | funnel 和 PostHog 事件需重新对齐                   | 看不到留存和转化瓶颈 | 定义 activation / retention / conversion 指标 |
| AI 成本    | Coach 成本与 promptGuard 需持续红队                | 成本失控或提示注入   | 增加红队测试和预算保护                        |
| 内容版权   | 公开仓库与题库来源仍需持续审计                     | 版权和分发风险       | 建内容来源台账和导入审核流程                  |

## 验证要求

- 文档-only 变更至少跑 `format:check`；若同步事实源，跑完整验证。
- 脚本逻辑变更必须补 `tests/scripts/*`。
- Coach 准入、配额、API 行为变更必须补 `tests/lib/coach/*` 与 `tests/api/coach.test.ts`。
- UI 关键路径变更必须补 `tests/app/*` 或 `tests/components/*`。
- 外部系统相关操作（Stripe、Supabase、邮件、部署、推送）必须先获得 Frank 明确批准。
